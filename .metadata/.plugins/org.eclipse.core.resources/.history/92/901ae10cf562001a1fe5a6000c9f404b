/*
 * my_it_callbacks.c
 *
 *  Created on: 10-03-2020
 *      Author: julio
 */


#include "my_it_callbacks.h"

uint8_t dat[4] = {'o','k','\r','\n'};

void HAL_UART_RxCpltCallback (UART_HandleTypeDef *huart)
{
  static uint8_t buffer[10];

 if(huart->Instance == USART2)
 {
   HAL_GPIO_WritePin(GPIOC,out1_Pin,GPIO_PIN_SET);
 }
 HAL_UART_Receive_IT(&huart2, buffer, 10); //activate UART receive interrupt
 HAL_UART_Transmit(&huart2, dat, 4, 10);
}

